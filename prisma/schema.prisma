generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

// است که به دلیل قدیمی بودن نسخه آن است. در عمل خطایی وجود ندارد prisma این خطا صرفا برای اکستنشن
datasource db {
  provider = "postgresql"
}
